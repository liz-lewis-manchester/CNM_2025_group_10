import numpy as np
class RiverModel:
   def_init_(self.length,,dx,total_time,dt,velocity):
   """
   Initialise model parameters.
   """
   self.L=length
   self.dx=dx
   self.T=dt
   #we will define the grid

   
